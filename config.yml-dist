jenkins-url: 'https://jenkins.company.com'
username: 'username'

# Create in user profile
token: 'token'

list:
  # Exclude from list command output
  exclude:
    - 'PROD-'
    - 'STAGING-'
    - 'TEST-'